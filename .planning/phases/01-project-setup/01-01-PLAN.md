---
phase: 01-project-setup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - .python-version
  - .gitignore
  - src/automated_data_cataloger/__init__.py
  - src/automated_data_cataloger/connection/__init__.py
  - src/automated_data_cataloger/schema/__init__.py
  - src/automated_data_cataloger/cataloging/__init__.py
  - src/automated_data_cataloger/storage/__init__.py
  - src/automated_data_cataloger/web/__init__.py
  - README.md
autonomous: true

must_haves:
  truths:
    - "Project has Python environment managed by uv"
    - "Python version is pinned to 3.12"
    - "Project uses src layout with five module directories"
    - "Git repository ignores Python cache and virtual environment files"
  artifacts:
    - path: "pyproject.toml"
      provides: "Project metadata and dependency definitions"
      contains: "[project]"
      min_lines: 20
    - path: ".python-version"
      provides: "Python version specification"
      contains: "3.12"
    - path: "src/automated_data_cataloger/__init__.py"
      provides: "Package initialization"
      contains: "__version__"
    - path: ".gitignore"
      provides: "Git ignore patterns for Python projects"
      contains: ".venv"
      min_lines: 50
  key_links:
    - from: "uv commands"
      to: "pyproject.toml"
      via: "dependency management"
      pattern: "\\[project\\]"
    - from: "Python imports"
      to: "src/automated_data_cataloger/"
      via: "package structure"
      pattern: "from automated_data_cataloger"
---

<objective>
Initialize Python project with modern tooling foundation using uv package manager and src layout structure.

Purpose: Establish project repository with proper dependency management, version pinning, and modular structure that supports the five distinct modules needed for database cataloging (connection, schema, cataloging, storage, web).

Output: Working Python project with uv-managed environment, src layout, and module structure ready for development.
</objective>

<execution_context>
@/Users/samitugal/.claude/get-shit-done/workflows/execute-plan.md
@/Users/samitugal/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-project-setup/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Initialize uv project with src layout and module structure</name>
  <files>
    pyproject.toml
    .python-version
    src/automated_data_cataloger/__init__.py
    src/automated_data_cataloger/connection/__init__.py
    src/automated_data_cataloger/schema/__init__.py
    src/automated_data_cataloger/cataloging/__init__.py
    src/automated_data_cataloger/storage/__init__.py
    src/automated_data_cataloger/web/__init__.py
  </files>
  <action>
Install uv if not present, then initialize project with src layout:

1. Check if uv is installed: `which uv` - if not found, install with: `curl -LsSf https://astral.sh/uv/install.sh | sh`
2. Initialize project: `uv init --package automated-data-cataloger`
3. Pin Python version: Create `.python-version` with content "3.12"
4. Install Python 3.12: `uv python install 3.12`
5. Update pyproject.toml to include:
   - `requires-python = ">=3.11"` in [project] section
   - `description = "Automatically document legacy databases using LLM-powered analysis"`
   - `authors = [{name = "Your Name", email = "your.email@example.com"}]` (placeholder values)

6. Create five module directories under src/automated_data_cataloger/:
   - connection/ - Database connection handling
   - schema/ - Schema extraction and analysis
   - cataloging/ - LLM-powered table cataloging
   - storage/ - Neo4j graph storage
   - web/ - Web interface

7. Create minimal __init__.py for each module:
   - Main package __init__.py: `__version__ = "0.1.0"` and docstring
   - Each module __init__.py: Docstring describing module purpose (from research Pattern 5)

Use src layout pattern from research (Pattern 1) to prevent import issues. All importable code goes under src/automated_data_cataloger/, not project root.
  </action>
  <verify>
1. `uv --version` returns version number
2. `ls -la` shows pyproject.toml, .python-version, src/ directory
3. `cat .python-version` shows "3.12"
4. `ls src/automated_data_cataloger/` shows five module directories
5. `cat pyproject.toml` shows [project] section with name, version, description, requires-python
6. Each module directory has __init__.py file
  </verify>
  <done>
- uv is installed and accessible
- pyproject.toml exists with project metadata
- .python-version pins Python to 3.12
- src/automated_data_cataloger/ contains five module directories (connection, schema, cataloging, storage, web)
- Each module has __init__.py with docstring
- Main package __init__.py declares __version__ = "0.1.0"
  </done>
</task>

<task type="auto">
  <name>Configure .gitignore and README</name>
  <files>
    .gitignore
    README.md
  </files>
  <action>
1. Download official Python .gitignore template:
   `curl https://raw.githubusercontent.com/github/gitignore/master/Python.gitignore > .gitignore`

2. Create README.md with project overview:
   - Project title: "Automated Data Cataloger"
   - Description: "Automatically document legacy databases using LLM-powered analysis"
   - Key features: PostgreSQL/MySQL support, AI-powered analysis, Neo4j storage, web visualization
   - Requirements: Python 3.11+, uv package manager
   - Quick start: Installation and basic usage instructions
   - Project structure: Brief overview of five modules

Keep README concise (research anti-pattern: documentation for documentation's sake). Focus on getting developers started quickly.
  </action>
  <verify>
1. `cat .gitignore` contains Python-specific patterns (.venv, __pycache__, *.pyc, .pytest_cache, .mypy_cache)
2. `cat README.md` shows project title, description, and basic setup instructions
3. `git status` does not show .venv, __pycache__, or cache directories if they exist
  </verify>
  <done>
- .gitignore exists with comprehensive Python ignore patterns
- README.md provides project overview and quick start guide
- Git will not track virtual environments or Python cache files
  </done>
</task>

</tasks>

<verification>
After task completion:
1. Run `uv sync` to create virtual environment and verify project setup
2. Verify all module directories exist: `find src -type d -name "__init__.py" -exec dirname {} \;`
3. Test Python import: `uv run python -c "import automated_data_cataloger; print(automated_data_cataloger.__version__)"`
4. Check git status: `git status` should show only tracked files, not .venv or cache
</verification>

<success_criteria>
- uv package manager is installed and manages project environment
- Project uses src layout with automated_data_cataloger package
- Five module directories exist with proper __init__.py files
- Python 3.12 is pinned and installed
- .gitignore prevents committing cache and virtual environment files
- README provides clear project overview
- Project can be imported: `import automated_data_cataloger` succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-setup/01-01-SUMMARY.md`
</output>
