---
phase: 01-project-setup
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - pyproject.toml
  - .pre-commit-config.yaml
  - .github/workflows/ci.yml
  - tests/conftest.py
autonomous: true

must_haves:
  truths:
    - "Code quality checks run automatically before commits"
    - "Tests can be executed with coverage tracking"
    - "CI pipeline validates code on every push to GitHub"
    - "Type checking catches errors before runtime"
  artifacts:
    - path: "pyproject.toml"
      provides: "Ruff, pytest, and mypy configuration"
      contains: "[tool.ruff]"
    - path: ".pre-commit-config.yaml"
      provides: "Pre-commit hook configuration"
      contains: "astral-sh/ruff-pre-commit"
      min_lines: 20
    - path: ".github/workflows/ci.yml"
      provides: "GitHub Actions CI/CD workflow"
      contains: "astral-sh/setup-uv"
      min_lines: 40
    - path: "tests/conftest.py"
      provides: "Shared pytest fixtures"
  key_links:
    - from: "git commit"
      to: ".pre-commit-config.yaml"
      via: "pre-commit hooks"
      pattern: "ruff-check"
    - from: "GitHub push"
      to: ".github/workflows/ci.yml"
      via: "CI trigger"
      pattern: "on:\\s*push:"
    - from: "pytest commands"
      to: "pyproject.toml"
      via: "test configuration"
      pattern: "\\[tool\\.pytest"
---

<objective>
Configure development tooling (Ruff, pytest, mypy) and CI/CD pipeline to automate code quality checks.

Purpose: Ensure consistent code quality through automated linting, formatting, type checking, and testing. Pre-commit hooks catch issues before commits, CI pipeline validates every push.

Output: Fully configured development environment with pre-commit hooks and GitHub Actions CI/CD running Ruff, pytest, and mypy.
</objective>

<execution_context>
@/Users/samitugal/.claude/get-shit-done/workflows/execute-plan.md
@/Users/samitugal/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-project-setup/01-RESEARCH.md
@.planning/phases/01-project-setup/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Configure Ruff, pytest, and mypy in pyproject.toml</name>
  <files>
    pyproject.toml
  </files>
  <action>
Add development dependencies and tool configurations to pyproject.toml:

1. Add dev dependencies using uv:
   - `uv add pytest pytest-cov` (runtime deps for testing)
   - `uv add --dev ruff mypy pre-commit` (development tools)

2. Add [tool.ruff] section to pyproject.toml:
   ```toml
   [tool.ruff]
   line-length = 88
   target-version = "py311"

   [tool.ruff.lint]
   select = ["E", "F", "I", "N", "W", "UP"]
   ignore = []
   ```

3. Add [tool.pytest.ini_options] section:
   ```toml
   [tool.pytest.ini_options]
   testpaths = ["tests"]
   python_files = ["test_*.py"]
   python_functions = ["test_*"]
   addopts = "--cov=src --cov-report=term-missing --cov-report=html"
   ```

4. Add [tool.mypy] section:
   ```toml
   [tool.mypy]
   python_version = "3.11"
   warn_return_any = true
   warn_unused_configs = true
   disallow_untyped_defs = true
   ```

Use exact configurations from research Pattern 2. Ruff replaces Black + Flake8 + isort per research standard stack. pytest-cov enables coverage tracking per research Pattern 4.
  </action>
  <verify>
1. `uv sync` completes without errors
2. `cat pyproject.toml` shows [tool.ruff], [tool.pytest.ini_options], and [tool.mypy] sections
3. `uv run ruff check .` runs successfully (may show no issues yet)
4. `uv run mypy --version` shows mypy is installed
5. `uv run pytest --version` shows pytest is installed
  </verify>
  <done>
- Ruff, pytest, mypy, and pre-commit are installed as dependencies
- pyproject.toml contains complete tool configurations
- Ruff is configured for Python 3.11+ with standard linting rules
- pytest is configured to run tests from tests/ directory with coverage
- mypy is configured for strict type checking
  </done>
</task>

<task type="auto">
  <name>Set up pre-commit hooks</name>
  <files>
    .pre-commit-config.yaml
  </files>
  <action>
Create .pre-commit-config.yaml with Ruff, mypy, and standard hooks:

```yaml
repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.15.2
    hooks:
      - id: ruff-check
        args: [--fix]
      - id: ruff-format

  - repo: https://github.com/pre-commit/mirrors-mypy
    rev: v1.13.0
    hooks:
      - id: mypy
        additional_dependencies: [types-all]

  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v5.0.0
    hooks:
      - id: trailing-whitespace
      - id: end-of-file-fixer
      - id: check-yaml
      - id: check-added-large-files
```

Then:
1. Install pre-commit hooks: `uv run pre-commit install`
2. Run initial check on all files: `uv run pre-commit run --all-files`

Use exact configuration from research Pattern 3. This runs Ruff linting + formatting, mypy type checking, and standard checks on every commit.
  </action>
  <verify>
1. `cat .pre-commit-config.yaml` shows all four repos configured
2. `git status` shows .git/hooks/pre-commit exists (created by pre-commit install)
3. `uv run pre-commit run --all-files` executes successfully
4. Make a trivial file change and `git add` it, then `git commit` should trigger pre-commit hooks
  </verify>
  <done>
- .pre-commit-config.yaml exists with Ruff, mypy, and standard hooks
- Pre-commit hooks are installed in git repository
- Running pre-commit on all files succeeds
- Git commits now trigger automated quality checks
  </done>
</task>

<task type="auto">
  <name>Create GitHub Actions CI/CD workflow and basic test structure</name>
  <files>
    .github/workflows/ci.yml
    tests/conftest.py
  </files>
  <action>
1. Create .github/workflows/ci.yml with uv-based workflow:

```yaml
name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.11", "3.12"]

    steps:
      - uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v7
        with:
          version: "latest"
          enable-cache: true

      - name: Set up Python ${{ matrix.python-version }}
        run: uv python install ${{ matrix.python-version }}

      - name: Install dependencies
        run: uv sync --locked --all-extras --dev

      - name: Run ruff check
        run: uv run ruff check .

      - name: Run ruff format
        run: uv run ruff format --check .

      - name: Run mypy
        run: uv run mypy src/

      - name: Run pytest
        run: uv run pytest tests/ --cov=src --cov-report=xml

      - name: Upload coverage
        uses: codecov/codecov-action@v4
        with:
          file: ./coverage.xml
        if: matrix.python-version == '3.12'
```

2. Create tests/conftest.py with basic pytest setup:
```python
"""Shared pytest fixtures for automated-data-cataloger tests."""

import pytest


@pytest.fixture
def sample_fixture():
    """Example fixture - replace with actual fixtures as needed."""
    return {"example": "data"}
```

Use exact workflow from research Pattern 4. Matrix testing ensures compatibility with both Python 3.11 and 3.12. astral-sh/setup-uv action provides fast cached dependency installation.
  </action>
  <verify>
1. `cat .github/workflows/ci.yml` shows complete workflow with all steps
2. `cat tests/conftest.py` shows pytest fixture setup
3. Validate YAML syntax: `uv run python -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))"`
4. Push to GitHub and verify workflow runs (or use `act` to test locally if available)
  </verify>
  <done>
- .github/workflows/ci.yml exists with complete CI pipeline
- Workflow runs on push to main and pull requests
- Matrix testing covers Python 3.11 and 3.12
- CI runs Ruff linting, Ruff formatting, mypy type checking, and pytest with coverage
- tests/conftest.py provides shared pytest fixture structure
- Coverage reports upload to Codecov on Python 3.12 runs
  </done>
</task>

</tasks>

<verification>
After task completion:
1. Verify all tools work locally:
   - `uv run ruff check .` (should pass or show fixable issues)
   - `uv run ruff format .` (formats code)
   - `uv run mypy src/` (type checks src directory)
   - `uv run pytest` (runs tests - may show no tests collected initially)

2. Verify pre-commit integration:
   - Create dummy file with trailing whitespace: `echo "test " > temp.txt && git add temp.txt`
   - Try to commit: `git commit -m "test"` should run hooks and fix whitespace

3. Verify CI workflow:
   - Push changes to GitHub
   - Check Actions tab for workflow run
   - Confirm all steps pass

4. Test coverage:
   - `uv run pytest --cov=src --cov-report=html`
   - `open htmlcov/index.html` to view coverage report
</verification>

<success_criteria>
- All development tools (Ruff, pytest, mypy, pre-commit) are installed and configured
- pyproject.toml contains complete tool configurations
- Pre-commit hooks run automatically on git commit
- GitHub Actions workflow validates code on every push
- Matrix testing covers Python 3.11 and 3.12
- Coverage tracking is enabled and generates reports
- All quality checks can be run locally with uv run commands
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-setup/01-02-SUMMARY.md`
</output>
