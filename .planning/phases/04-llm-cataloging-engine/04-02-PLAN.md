---
phase: 04-llm-cataloging-engine
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data_cataloger/cataloging/client.py
  - tests/cataloging/test_client.py
  - pyproject.toml
autonomous: true
requirements: [CATL-07]

user_setup:
  - service: openai
    why: "LLM-powered table cataloging"
    env_vars:
      - name: OPENAI_API_KEY
        source: "OpenAI Platform -> API keys -> Create new secret key"
    dashboard_config: []

must_haves:
  truths:
    - "OpenAI client automatically retries on rate limits"
    - "API key loaded from environment variable"
    - "Structured outputs return typed Pydantic objects"
  artifacts:
    - path: "src/data_cataloger/cataloging/client.py"
      provides: "OpenAI client wrapper with retry logic"
      min_lines: 50
      exports: ["CatalogClient"]
    - path: "tests/cataloging/test_client.py"
      provides: "Client initialization and retry tests"
      min_lines: 60
  key_links:
    - from: "src/data_cataloger/cataloging/client.py"
      to: "openai.OpenAI"
      via: "Client initialization with API key from environment"
      pattern: "from openai import OpenAI"
    - from: "src/data_cataloger/cataloging/client.py"
      to: "tenacity retry decorator"
      via: "Automatic retry on RateLimitError"
      pattern: "@retry\\("
---

<objective>
Set up OpenAI API client with structured outputs and automatic retry logic

**Purpose:** Establish reliable LLM integration with GPT-4o using OpenAI's Structured Outputs for guaranteed schema compliance and tenacity for production-grade retry handling.

**Output:** CatalogClient wrapper class that manages OpenAI API calls with exponential backoff retry, environment-based API key loading, and type-safe response parsing.
</objective>

<execution_context>
@/Users/samitugal/.claude/get-shit-done/workflows/execute-plan.md
@/Users/samitugal/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-llm-cataloging-engine/04-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add OpenAI and retry dependencies</name>
  <files>pyproject.toml</files>
  <action>
Add required dependencies to pyproject.toml:

In `[project]` dependencies array, add:
- `openai>=2.21.0` - OpenAI Python SDK with structured outputs support
- `tenacity>=9.0.0` - Retry with exponential backoff
- `python-dotenv>=1.0.0` - Environment variable loading from .env files

Do NOT add tiktoken yet (defer to cost optimization phase if needed).

Run `uv sync` after updating to install new dependencies.
  </action>
  <verify>uv run python -c "import openai; import tenacity; import dotenv; print('Dependencies imported successfully')"</verify>
  <done>openai, tenacity, and python-dotenv are installed and importable</done>
</task>

<task type="auto">
  <name>Task 2: Create OpenAI client wrapper with retry</name>
  <files>src/data_cataloger/cataloging/client.py</files>
  <action>
Create CatalogClient class following research Pattern 2 and Pattern 3:

1. **CatalogClient class**:
   - `__init__(self)`: Initialize OpenAI client (loads OPENAI_API_KEY from environment automatically)
   - Verify API key is set: `if not os.getenv("OPENAI_API_KEY"): raise ValueError("OPENAI_API_KEY not found")`
   - Store client as `self.client = OpenAI()` (no explicit api_key parameter - reads from env)

2. **analyze_table method with retry decorator**:
   ```python
   @retry(
       wait=wait_random_exponential(min=1, max=60),
       stop=stop_after_attempt(6),
       retry=retry_if_exception_type(RateLimitError)
   )
   def analyze_table(self, messages: list[dict[str, str]], response_model: type[BaseModel]) -> BaseModel:
       """Analyze table with automatic retry on rate limits.

       Args:
           messages: List of message dicts with role and content
           response_model: Pydantic model class for structured output

       Returns:
           Parsed Pydantic instance from LLM response
       """
   ```
   - Use `client.beta.chat.completions.parse()` (not create())
   - Model: `"gpt-4o-2024-08-06"` (required for structured outputs per research)
   - `response_format=response_model`
   - Return `response.choices[0].message.parsed`

3. **Imports**:
   - `import os`
   - `from openai import OpenAI, RateLimitError`
   - `from tenacity import retry, stop_after_attempt, wait_random_exponential, retry_if_exception_type`
   - `from pydantic import BaseModel`

Add comprehensive docstring explaining:
- Why gpt-4o-2024-08-06 specifically (structured outputs requirement)
- Retry strategy (exponential backoff 1-60s, max 6 attempts)
- Environment variable requirement (OPENAI_API_KEY)

Use type annotations compatible with mypy --strict.
  </action>
  <verify>uv run mypy --strict src/data_cataloger/cataloging/client.py</verify>
  <done>client.py exports CatalogClient with analyze_table method, passes mypy strict, handles missing API key gracefully</done>
</task>

<task type="auto">
  <name>Task 3: Write client tests with mocking</name>
  <files>tests/cataloging/test_client.py</files>
  <action>
Create tests following research mocking patterns:

1. **test_client_initialization_success**:
   - Mock `os.getenv("OPENAI_API_KEY")` to return fake key
   - Instantiate CatalogClient, verify no exception

2. **test_client_initialization_missing_api_key**:
   - Mock `os.getenv("OPENAI_API_KEY")` to return None
   - Verify CatalogClient() raises ValueError with message about OPENAI_API_KEY

3. **test_analyze_table_success**:
   - Create simple Pydantic model for testing (e.g., TestResponse with text: str field)
   - Mock OpenAI client's beta.chat.completions.parse() to return mock response
   - Call analyze_table with test messages and TestResponse model
   - Verify returned object is TestResponse instance with expected data

4. **test_analyze_table_retry_on_rate_limit**:
   - Mock parse() to raise RateLimitError first 2 times, succeed on 3rd
   - Verify analyze_table eventually succeeds (proves retry works)
   - Verify parse() called 3 times

5. **test_analyze_table_retry_exhaustion**:
   - Mock parse() to always raise RateLimitError
   - Verify analyze_table raises RateLimitError after 6 attempts
   - Verify parse() called exactly 6 times

Use `unittest.mock.patch` for mocking OpenAI client and os.getenv.
Use `unittest.mock.Mock` for creating response objects.
Follow pytest patterns from Phase 3.

Note: These are unit tests with mocks - no real API calls, no cost.
  </action>
  <verify>uv run pytest tests/cataloging/test_client.py -v --cov=src/data_cataloger/cataloging/client</verify>
  <done>5 tests pass with >95% coverage on client.py, validating initialization, retry logic, and error handling</done>
</task>

<task type="auto">
  <name>Task 4: Verify OpenAI integration</name>
  <files>None (verification only)</files>
  <action>
Run comprehensive verification:
1. Import check: `from data_cataloger.cataloging.client import CatalogClient`
2. Type checking: mypy --strict on client.py and test_client.py
3. Code quality: ruff check and ruff format
4. Test suite: pytest with coverage report

Verify:
- CatalogClient can be instantiated (with mocked API key in tests)
- Missing API key raises clear ValueError
- Mock tests prove retry decorator is configured correctly
- Type annotations pass strict mypy
- No real API calls in tests (all mocked)

Do NOT make real OpenAI API calls - user will set up API key during execution phase.
  </action>
  <verify>
uv run pytest tests/cataloging/test_client.py -v &&
uv run mypy --strict src/data_cataloger/cataloging/client.py tests/cataloging/test_client.py &&
uv run ruff check src/data_cataloger/cataloging/
  </verify>
  <done>All tests pass, mypy strict passes, client ready for use in cataloging agent, no real API calls made</done>
</task>

</tasks>

<verification>
- [ ] pyproject.toml includes openai>=2.21.0, tenacity>=9.0.0, python-dotenv>=1.0.0
- [ ] CatalogClient initializes OpenAI client from OPENAI_API_KEY environment variable
- [ ] Missing API key raises ValueError with clear message
- [ ] analyze_table method uses beta.chat.completions.parse() with gpt-4o-2024-08-06
- [ ] Retry decorator configured with exponential backoff 1-60s, max 6 attempts, RateLimitError only
- [ ] Tests use mocks (no real API calls, no cost)
- [ ] Tests prove retry logic works (multiple attempts on RateLimitError)
- [ ] >95% test coverage on client.py
- [ ] mypy --strict passes
- [ ] No ruff violations
</verification>

<success_criteria>
Client is complete when:
1. CatalogClient provides type-safe OpenAI API wrapper
2. Retry logic automatically handles rate limits with exponential backoff
3. API key loading from environment is validated at initialization
4. Structured outputs return typed Pydantic objects (not raw JSON)
5. Tests prove retry behavior without making real API calls
6. Ready for integration in cataloging agent (Plan 04-04)
7. User setup documentation captures required OPENAI_API_KEY configuration
</success_criteria>

<output>
After completion, create `.planning/phases/04-llm-cataloging-engine/04-02-SUMMARY.md`
</output>
